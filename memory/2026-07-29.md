
## 08:12 -- Implement box snapping and group drag
Implemented box-to-box and image-border snapping for move and resize interactions, including group dragging based on the selected boxes' bounding box. Added snapToEdges and snapThreshold props with defaults, and exposed the toggle and threshold slider in the example Interaction Settings UI. Verified library typecheck/build and example build.
